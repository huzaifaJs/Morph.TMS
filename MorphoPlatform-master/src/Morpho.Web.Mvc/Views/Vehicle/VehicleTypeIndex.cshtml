@using Morpho.Web.Models.Vehichle
@model VehicleTypeViewModel
@using Morpho.Web.Startup

@{
    ViewBag.Title = L("VehicleTypes");
    ViewBag.CurrentPageName = PageNames.VehicleType;
}

@section scripts
{
    <script src="~/view-resources/Views/VehicleType/Index.js" asp-append-version="true"></script>
}

<section class="content-header">
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-6">
                <h1>@L("VehicleTypes")</h1>
            </div>

            <div class="col-sm-6">
                <a href="javascript:;" data-toggle="modal" data-target="#CreateVehicleTypeModal"
                   class="btn bg-blue float-sm-right">
                    <i class="fa fa-plus-square"></i>
                    @L("Create")
                </a>
            </div>
        </div>
    </div>
</section>

<section class="content">
    <div class="container-fluid">
        <div class="card">

            <div class="card-body">
                <div class="table-responsive">
                    <table id="VehicleTypeTable" class="table table-striped table-bordered">
                        <thead>
                            <tr>
                                <th>@L("VehicleTypeName")</th>
                                <th>@L("VehicleTypesRemark")</th>
                                <th>@L("VehicleTypesIsActive")</th>
                                <th>@L("VehicleTypesActions")</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
            </div>

        </div>
    </div>
</section>

@await Html.PartialAsync("~/Views/Vehicle/_CreateModal.cshtml")
<div id="EditVehicleTypeModal" class="modal fade">
    <div class="modal-dialog modal-lg">
        <div class="modal-content"></div>
    </div>
</div>


