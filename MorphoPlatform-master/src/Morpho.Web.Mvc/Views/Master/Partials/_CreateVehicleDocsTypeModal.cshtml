@using Morpho.Web.Models.Common.Modals
@model Morpho.Web.Models.VehicleDocsTypeViewModel
@{
    Layout = null;
}

<div class="modal fade" id="CreateVehicleDocsTypeModal" tabindex="-1" role="dialog" aria-labelledby="CreateVehicleDocsTypeModalLabel" data-backdrop="static">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">

            @await Html.PartialAsync("~/Views/Shared/Modals/_ModalHeader.cshtml",
            new ModalHeaderViewModel(L("VehicleDocsTypesCreateVehicleType")))

            <form name="CreateVehicleDocsType" method="post" role="form" class="form-horizontal">

                <div class="modal-body">

                    <!-- VEHICLE TYPE NAME -->
                    <div class="form-group row required">
                        <label class="col-md-3 col-form-label">@L("VehicleDocsTypes")</label>
                        <div class="col-md-9">
                            <input type="text"
                                   class="form-control"
                                   name="document_type_name"
                                   required
                                   maxlength="250"
                                   placeholder="@L("VehicleDocsTypeName")" />
                        </div>
                    </div>
                    

                    <!-- REMARK -->
                    <div class="form-group row required">
                        <label class="col-md-3 col-form-label">@L("VehicleDocsTypesRemark")</label>
                        <div class="col-md-9">
                            <textarea class="form-control"
                                      name="description"
                                      required
                                      maxlength="600"
                                      placeholder="@L("VehicleDocsTypesRemark")"></textarea>
                        </div>
                    </div>

                </div>

                @await Html.PartialAsync("~/Views/Shared/Modals/_ModalFooterWithSaveAndCancel.cshtml")

            </form>

        </div>
    </div>
</div>
